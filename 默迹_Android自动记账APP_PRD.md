# 默迹（Moji）Android 自动记账 APP 产品需求文档（PRD）

> **产品口号：** 让每一笔消费，悄悄留下轨迹。  
> **文档版本：** v1.0  
> **文档日期：** 2026-08-24  
> **产品阶段：** MVP / V1 需求冻结稿  
> **首发平台：** Android  
> **核心原则：** Local First、无账号完整可用、免费、无广告、自动记账优先、手动记账兜底

---

## 0. 文档目的与决策口径

本文档用于统一产品、设计、Android 开发、测试和后续运营对“默迹”V1 的理解，可直接作为需求拆分、原型设计、数据库设计和验收依据。

本文将需求分为三种状态：

- **已确认：** 对话中已经明确选择，V1 按此实现。
- **工程建议：** 为使需求可开发而补充的默认值，可在技术评审时调整，但不得改变产品目标。
- **后续项：** 数据模型可预留，但不进入当前版本验收。

若本文中的营销表达与能力边界冲突，以“能力边界、状态机和验收标准”为准。默迹追求尽可能高的捕获率，不承诺所有设备、系统版本和支付页面下 100% 无漏单。

---

## 1. 产品背景与愿景

### 1.1 背景

微信支付和支付宝已经覆盖大量日常消费，但传统记账要求用户在付款后再次打开记账应用、输入金额、选择分类、填写备注。真正阻碍用户长期记账的通常不是需求不足，而是每笔交易的重复操作成本。

现有产品常见问题：

- 自动能力不足，仍依赖用户主动输入、截图或导入账单。
- 每笔自动识别后仍要求确认，把“手动记账”变成“手动确认”。
- 强制登录或依赖云端，用户难以确认消费数据如何被使用。
- 广告、会员墙或复杂资产功能抢占核心记账体验。

### 1.2 产品愿景

> 让记账变成一件“不需要记得”的事情。

用户照常使用微信或支付宝付款，默迹在已适配场景内自动捕获并在本地形成账单。高可信交易直接入账；只有不确定、转账或字段缺失等情况才请求用户处理。用户月底打开应用即可看到消费全景，并通过偶尔修正让本地规则越来越懂自己的消费习惯。

### 1.3 北极星体验

```text
用户完成付款
    ↓
默迹识别、去重、分类并写入本地账本
    ↓
显示 3 秒轻量卡片：已记入默迹
    ↓
用户无需操作，继续使用手机
```

判断任何新增需求时，优先回答：

> 它是否让“付款 → 完成记账”更无感、更可靠或更可纠正？

---

## 2. 产品定位

### 2.1 一句话定位

默迹是一款 Android 首发、以微信和支付宝自动捕获为核心的 Local First 个人记账工具。

### 2.2 产品属性

| 维度 | 定位 |
|---|---|
| 核心场景 | 微信 / 支付宝付款后的自动记账 |
| 使用方式 | 自动记账为主，手动记账兜底 |
| 数据策略 | 本地数据库为唯一核心数据源 |
| 账号策略 | 无账号、无网络也可完整使用 |
| 商业模式 | 免费、无广告；V1 不设置会员墙 |
| 视觉气质 | Apple 式克制、留白、大数字、弱边框、流畅动画 |
| 用户关系 | 系统先提供基础规则，用户纠正后在本地持续学习 |
| 首发平台 | Android；iOS 不承诺同等自动捕获能力 |

### 2.3 核心价值

1. **无感：** 高可信支付成功后自动入账，不要求逐笔确认。
2. **可靠：** 多事件聚合、交易去重、字段置信度和异常分流共同降低错账与重复账。
3. **可控：** 每条账单可修改、撤销、删除；自动分类规则可查看、禁用和删除。
4. **私密：** 识别、分类、统计和存储默认均在设备本地完成。
5. **可持续：** 免费无广告，数据可备份、恢复及导出，不锁定用户。

---

## 3. 目标用户与典型场景

### 3.1 目标用户

**核心用户**

- 主要使用 Android 手机。
- 日常消费高度依赖微信支付和支付宝。
- 想了解钱花在哪里，但无法坚持手动记账。
- 重视隐私、反感强制登录、广告和订阅墙。
- 接受一次性配置无障碍等系统能力，以换取后续自动化。

**次级用户**

- 已经使用传统记账应用，但希望减少录入操作的人。
- 希望导出 CSV / Excel 做个人分析的人。
- 愿意通过本地商户规则长期训练个人分类体系的人。

### 3.2 典型场景

| 场景 | 期望结果 |
|---|---|
| 线下扫码、付款码支付 | 支付成功后自动记录金额、商户、平台和时间 |
| 美团、淘宝、滴滴等线上消费 | 若最终通过已适配的微信 / 支付宝页面完成，尽可能捕获 |
| 现金、银行 APP 直接支付 | 通过手动记账补录 |
| 收到退款 | 记录退款资金事件，并尽可能关联原消费 |
| 收到红包 | 默认记为收入，可设置不计入正式收入统计 |
| 收到或发出转账 | V1 默认进入待确认，避免误判为消费或收入 |
| 换手机 | 从手动备份文件恢复完整账本与规则 |
| 漏单排查 | 在“自动记账”健康页查看服务状态和最小化诊断记录 |

---

## 4. 产品目标、成功指标与非目标

### 4.1 V1 产品目标

- 在已适配的微信 / 支付宝成功支付页面中，尽可能自动生成一条且仅一条正确交易。
- 高可信交易无需确认直接入账；低可信或特殊交易明确分流。
- 提供足以替代普通个人记账应用的账本、手动记账、搜索、统计和预算能力。
- 无账号、无网络时所有 V1 核心功能完整可用。
- 用户可备份、恢复并导出自己的数据。

### 4.2 建议的核心指标

指标必须按“平台 × 支付场景 × APP 版本 × Android / 厂商版本”分层，禁止只报一个总体数字。

| 指标 | 定义 | V1 建议目标 |
|---|---|---:|
| 已适配场景捕获率 | 成功捕获交易数 / 测试集中真实成功支付数 | ≥ 95% |
| 精确入账率 | 金额、方向、平台正确且未重复的自动入账数 / 自动入账数 | ≥ 98% |
| 重复账率 | 同一真实交易生成多条有效账单的次数 / 捕获交易数 | ≤ 0.5% |
| 高可信自动入账占比 | 无需用户确认的捕获交易数 / 捕获交易数 | ≥ 85% |
| 手动修正率 | 用户修改分类或字段的自动账单数 / 自动账单数 | 持续下降 |
| 崩溃率 | 无崩溃会话占比 | ≥ 99.5% |
| 本地备份可恢复率 | 通过校验的备份成功恢复比例 | 100% |

上述为产品目标而非对所有真实设备的公开承诺；正式数值需在兼容性样本建立后冻结。

### 4.3 V1 非目标

- 不做 iOS 等价的全自动捕获。
- 不直接读取微信、支付宝、银行卡账户余额。
- 不做银行 APP、信用卡账单、现金消费的自动捕获。
- 不做完整资产负债管理、投资组合或账户余额对账。
- 不做拆账；仅在数据结构中预留。
- 不做云端 AI 分类或上传消费数据训练模型。
- 不做应用锁；V1 仅提供“隐藏最近任务预览”的可选设置。
- 不做 PDF 月报。
- 不做自定义预算周期和自定义提醒阈值。
- 不做自动 AA 匹配；允许用户在待确认中标记 AA 回款。
- 不保证第三方支付 APP 更新后无需适配即可继续工作。

---

## 5. 版本范围与优先级

### 5.1 技术 MVP（P0）

用于证明核心链路成立，不等同于公开 V1：

1. 微信、支付宝各至少一个主流成功支付页面解析器。
2. Accessibility 事件采集、页面识别、字段解析与候选交易生成。
3. 金额、商户、平台、时间、方向的基础模型。
4. 0～8 秒 / 8～30 秒 / 30 秒以上的去重策略。
5. 高可信自动入账、待确认、解析失败三路分流。
6. 本地数据库、账本首页、账单修改 / 删除 / 撤销。
7. 极简手动记账。
8. 本地系统分类和基础商户规则。

### 5.2 产品 V1（P0 + P1）

在 MVP 上增加：

- 微信 / 支付宝主要场景矩阵与版本化解析规则。
- 可选通知辅助识别。
- 自动记账轻量卡片及可配置时长。
- 待确认列表与识别失败补录入口。
- 收入、转账、退款、部分退款和红包处理。
- 全局搜索与组合筛选。
- 月 / 年统计、趋势与分类条形图。
- 总预算、分类预算及 80% / 100% / 120% 提醒。
- 分类管理、用户商户规则管理。
- 手动备份 / 恢复、CSV / XLSX 导出。
- 自动记账健康页、隐私设置、诊断日志。
- 浅色 / 深色 / 跟随系统。

### 5.3 后续优先级

- **P2（V1.x）：** WebDAV / 用户自有云加密备份、规则包更新、更多页面适配、AA 回款关联、自定义预算周期。
- **P3（V2+）：** 官方端到端加密同步、多设备冲突处理、自然语言查询、拆账、更多支付渠道。

---

## 6. 信息架构

### 6.1 一级导航

底部固定三个入口：

```text
账本 ｜ 统计 ｜ 我的
```

手动记账入口放在账本首页右上角“＋”，搜索入口放在其左侧。预算不占一级导航。

### 6.2 页面树

```text
默迹
├── 首次启动
│   ├── 欢迎与价值说明
│   ├── 无障碍用途披露与同意
│   ├── 开启自动记账
│   ├── 悬浮卡片说明与授权
│   ├── 可选通知辅助识别
│   ├── 厂商后台运行引导
│   └── 配置结果
├── 账本
│   ├── 月份切换
│   ├── 月支出 / 今日支出 / 预算剩余
│   ├── 按日分组账单流
│   ├── 账单详情 Bottom Sheet
│   ├── 待确认
│   ├── 搜索与筛选
│   └── 手动记账
├── 统计
│   ├── 月 / 年切换
│   ├── 支出趋势
│   ├── 分类排行 / 条形图
│   ├── 收入
│   └── 收支结余
└── 我的
    ├── 自动记账
    │   ├── 微信 / 支付宝支持状态
    │   ├── 无障碍状态
    │   ├── 通知辅助识别
    │   ├── 悬浮卡片
    │   ├── 后台运行状态
    │   └── 最近识别摘要
    ├── 分类管理
    ├── 自动分类规则
    ├── 预算
    ├── 数据与备份
    │   ├── 创建备份
    │   ├── 从备份恢复
    │   ├── CSV 导出
    │   └── Excel 导出
    ├── 外观
    ├── 隐私
    └── 诊断日志
```

---

## 7. 首次启动与权限引导

### 7.1 原则

- 不强制注册或登录。
- 渐进式申请权限，不在首屏连续索取全部权限。
- 在跳转系统设置前，先用一屏明确说明“访问什么、为何访问、如何使用、是否上传”。
- 拒绝可选权限后仍能使用其他能力；拒绝无障碍后仍可手动记账。
- 用户可随时在“我的 → 自动记账”中重新配置。

### 7.2 推荐流程

1. **欢迎页**
   - 文案：微信 / 支付宝付款后，尽可能自动记入本地账本。
   - 明示：免费、无广告、无需账号。
2. **核心能力说明**
   - 说明无障碍服务仅用于识别用户主动打开的微信 / 支付宝支付相关界面。
   - 明示不会读取密码、验证码，不会代替用户点击或支付，不上传交易内容。
   - 用户单独勾选同意后才跳转系统设置。
3. **开启自动记账**
   - 返回应用后验证服务是否真正启用。
4. **开启悬浮卡片**
   - 解释用于支付后显示“已记账 / 修改 / 撤销”。
   - 可跳过；跳过时降级为应用内记录或系统通知提示。
5. **通知辅助识别（可选）**
   - 首次配置时推荐开启并解释用途，可跳过。
   - 仅处理微信 / 支付宝相关通知，作为交叉验证来源。
6. **后台运行引导**
   - 检测电池优化、自启动等状态。
   - 按厂商提供说明，不承诺可以自动完成设置。
7. **配置完成**
   - 展示当前状态与“先手动记一笔”入口。

### 7.3 权限降级矩阵

| 能力状态 | 自动捕获 | 辅助校验 | 浮层卡片 | 手动记账 / 账本 |
|---|---:|---:|---:|---:|
| 全部开启 | 是 | 是 | 是 | 是 |
| 无通知访问 | 是 | 否 | 是 | 是 |
| 无悬浮窗 | 是 | 可选 | 否，使用通知或应用内提示 | 是 |
| 无障碍关闭 | 否 | 通知不得单独作为默认强保证 | 否 | 是 |
| 离线 | 是 | 是 | 是 | 是 |

### 7.4 服务异常提示

- 正常时首页不显示运行状态。
- 无障碍关闭、服务断开或后台长期不可用时，首页展示一条非模态提示：`自动记账暂时不可用，点击检查`。
- 同一异常不得频繁弹窗；状态恢复后自动消失。

---

## 8. 自动记账完整流程

### 8.1 总体管线

```text
AccessibilityEvent / 可选支付通知
    ↓
来源白名单与事件节流
    ↓
页面快照（仅内存、最小字段）
    ↓
平台识别 + 页面类型识别
    ↓
版本化 Parser 提取字段
    ↓
候选事件聚合
    ↓
交易状态判断
    ↓
去重与现有交易匹配
    ↓
置信度与完整性判断
    ↓
分类 / 商户规则
    ↓
自动入账 | 待确认 | 解析失败提示 | 仅诊断
    ↓
本地持久化 + 轻量反馈
```

### 8.2 采集限制

- 仅监听明确白名单包名及必要事件类型。
- 仅在疑似支付相关页面进一步遍历节点树。
- 不保存整棵节点树、屏幕截图或无关页面文本。
- V1 不以 OCR 作为默认捕获路径。
- 不读取或存储密码、验证码、聊天内容、联系人或浏览器存储。
- 所有解析规则应包含平台、页面类型、适用版本、字段证据和回归样本。

### 8.3 候选交易最小字段

形成可入账候选至少需要：

- 金额可确定；
- 平台可确定；
- 资金方向可确定或可进入待确认；
- 成功状态可确定；
- 事件时间可确定。

商户允许未知。支付方式允许为 `UNKNOWN`，禁止为追求完整而猜测。

### 8.4 结果分流

| 条件 | 处理 |
|---|---|
| 明确成功、金额可靠、方向可靠、未重复 | 高可信自动入账 |
| 金额可靠但商户缺失 | 自动入账，商户显示“未知商户” |
| 转入 / 转出性质无法判断 | 进入待确认 |
| 商户已识别但金额缺失 | 不入账；提示识别失败，小字显示商户，并提供补金额入口 |
| 支付失败、取消、余额不足 | 不入账；仅保留最小诊断摘要 |
| 疑似重复且证据充分 | 合并到已有候选 / 交易，不新建账单 |
| 证据不足且可能不是交易 | 不入账；必要时静默记录匿名失败码 |

### 8.5 自动记账反馈卡片

默认显示 3 秒，可在设置中选择：关闭、2 秒、3 秒、5 秒、保持至关闭。

```text
✓ 已记入默迹

瑞幸咖啡                 ¥16.00
咖啡奶茶 · 微信

撤销                       修改
```

- 默认不震动。
- “撤销”仅撤销此次自动入账，不删除原始诊断摘要。
- “修改”打开账单详情 Bottom Sheet。
- 卡片显示期间可操作；超时消失后仍可在账本中修改。
- 普通用户不看到数值置信度。

### 8.6 识别失败卡片

```text
未能读取交易金额

瑞幸咖啡
检测到一笔微信支付

忽略                   补充金额
```

- 点击“补充金额”进入预填商户、平台和时间的手动记账页。
- 点击“忽略”后不进入账本，并记录用户处理结果，避免反复提示同一候选。

---

## 9. 交易状态、去重与幂等逻辑

### 9.1 状态定义

```text
DETECTED            已检测到疑似支付事件
COLLECTING          正在聚合同一页面的多个事件
PARSED              已提取候选字段
DUPLICATE_SUPPRESSED 已判定为既有交易的重复事件
AUTO_POSTED         高可信并已自动入账
PENDING_REVIEW      等待用户确认或补充
PARSE_FAILED        无法形成有效候选
PAYMENT_FAILED      支付失败或取消
IGNORED             用户忽略
CORRECTED           用户已修正
DELETED             软删除，可在撤销窗口恢复
FINALIZED           撤销窗口结束，正常有效
```

### 9.2 指纹字段

建议生成两级指纹：

**事件指纹**：`platform + pageType + windowId/pageInstance + normalizedVisibleFields + shortTimeBucket`

**交易指纹**：`platform + direction + amountMinor + normalizedMerchant + paymentStatus + timeBucket + optionalOrderId`

其中：

- 金额必须以最小货币单位整数保存，例如 ¥16.80 保存为 `1680`。
- 商户名先去空格、全半角差异和常见装饰字符，但原始显示值必须保留。
- 若能稳定读取平台订单号，应保存其不可逆摘要，并将其作为最高优先级去重证据；不得把完整订单号写入诊断日志。

### 9.3 时间窗口规则

| 间隔 | 默认判断 |
|---|---|
| 0～8 秒 | 高度疑似同一次交易；优先聚合事件，除非存在两个不同订单证据 |
| 8～30 秒 | 结合页面实例、成功状态迁移、通知证据和订单标识判断 |
| >30 秒 | 默认视为新交易，即使商户和金额相同 |

“商户 + 金额相同”不能单独作为重复依据，必须允许用户在短时间内进行两笔相同金额的真实支付。

### 9.4 幂等要求

- 候选交易写入和“最终交易创建”必须在单个本地数据库事务内完成。
- 同一 `dedupeKey` 只能拥有一条有效交易；冲突时更新证据，不新增账单。
- Accessibility 与通知来源命中同一交易时合并证据，不生成两条交易。
- 解析器重跑、应用重启或服务重连不得重复创建已落账交易。
- 删除为软删除；撤销窗口结束后可延迟清理，但统计必须立即排除。

---

## 10. 置信度与异常处理

### 10.1 内部评分维度

置信度仅供内部决策，不直接显示给普通用户。建议评分维度：

- 包名与平台匹配度。
- 页面类型匹配度。
- 成功 / 失败状态证据。
- 金额字段唯一性与格式可靠性。
- 商户字段可靠性。
- 收支方向可靠性。
- 支付方式字段可靠性。
- Accessibility 与通知证据是否一致。
- 是否命中已验证解析模板。
- 是否存在冲突字段或异常跳变。

### 10.2 建议阈值（工程可配置）

- `score >= 0.85` 且满足入账硬条件：自动入账。
- `0.60 <= score < 0.85`：待确认。
- `score < 0.60`：不入账；若已知商户但缺金额，显示补录提示。

硬条件优先于总分。例如金额缺失时不得因为其他字段得分高而自动入账。

### 10.3 异常矩阵

| 异常 | 用户结果 | 系统结果 |
|---|---|---|
| 金额缺失 | 提示补录 | `PARSE_FAILED` |
| 商户缺失 | 显示“未知商户”并入账 | `AUTO_POSTED` |
| 支付方式缺失 | 不提示错误 | 保存 `UNKNOWN` |
| 成功状态不明确 | 进入待确认或等待更多事件 | `COLLECTING/PENDING_REVIEW` |
| 支付失败 / 取消 | 账本无记录 | `PAYMENT_FAILED` 诊断摘要 |
| 字段来源冲突 | 待确认 | 保存冲突证据码 |
| 无障碍服务断开 | 首页异常提示 | 健康状态置为暂停 |
| 第三方 APP 页面改版 | 不错误入账 | 解析器失败率告警、需要规则适配 |

---

## 11. 微信 / 支付宝识别策略

### 11.1 总体策略

- AccessibilityService 为主数据源。
- NotificationListenerService 为用户可选的辅助数据源。
- 两类来源只在设备本地进行匹配。
- Parser 必须按“平台 → 页面类型 → 版本 / 特征”注册，不允许一个大正则处理全部页面。

### 11.2 页面类型建议

```text
WECHAT
├── PAYMENT_SUCCESS
├── TRANSFER_OUT
├── TRANSFER_IN
├── RED_PACKET_IN
├── REFUND
└── PAYMENT_FAILED

ALIPAY
├── PAYMENT_SUCCESS
├── TRANSFER_OUT
├── TRANSFER_IN
├── RED_PACKET_IN
├── REFUND
└── PAYMENT_FAILED
```

每个页面类型维护：

- 页面特征词及结构特征。
- 金额、商户、支付方式、状态、方向提取规则。
- 必需字段和可选字段。
- 适用 APP 版本范围。
- 正向、反向和边界回归样本。

### 11.3 解析优先级

1. 稳定节点 ID / contentDescription（若存在且可靠）。
2. 节点层级关系与邻近标签。
3. 页面特征词 + 位置无关的字段规则。
4. 通知内容只作为补充或交叉验证。
5. V1 不默认使用截图 OCR。

### 11.4 支付方式

可选枚举：

```text
WECHAT_BALANCE
ALIPAY_BALANCE
BANK_CARD
CREDIT_CARD
HUABEI
OTHER
UNKNOWN
```

只有页面明确暴露时才记录具体方式；否则保存 `UNKNOWN`。

### 11.5 版本适配与回归

- 每条解析规则必须带版本号和样本来源。
- 新规则发布前对历史样本全量回归。
- 规则更新不得读取或上传用户交易正文；V1 可随应用版本发布规则。
- 后续可设计“匿名规则版本检查”，但必须与账单数据严格隔离并允许关闭。

---

## 12. 自动分类与商户规则

### 12.1 V1 分类策略

V1 采用“用户规则优先 + 系统规则兜底”的纯本地规则方案，不依赖云端 AI。

优先级从高到低：

1. 用户精确商户规则。
2. 用户品牌级规则。
3. 系统精确商户规则。
4. 系统品牌 / 关键词规则。
5. 页面类型默认分类。
6. “其他”。

### 12.2 用户纠正后的学习

当用户修改自动账单分类时，询问：

```text
记住这次分类？

○ 仅“瑞幸咖啡（吉林大学南湖店）”
○ 所有“瑞幸咖啡”
○ 不记住
```

- 选择后立即创建或更新本地用户规则。
- 用户规则永远覆盖系统规则。
- 规则可编辑、禁用和删除。
- V1 使用用户显式选择，不在后台静默扩大品牌匹配范围。

### 12.3 默认分类体系

- 餐饮：正餐、外卖、咖啡奶茶、零食
- 交通：公交地铁、打车、火车飞机、其他交通
- 购物：日用品、数码、服饰、网购
- 娱乐：游戏、影视、旅游、其他娱乐
- 生活：水电燃气、通讯、房租、其他生活
- 学习
- 医疗
- 人情
- 其他

用户可以新增分类、改名、换图标、调整顺序。系统分类只能隐藏，不能物理删除；用户自定义分类允许删除，但删除前必须要求选择历史交易迁移目标。

---

## 13. 手动记账

### 13.1 定位

手动记账用于现金、银行 APP、漏单和识别失败补录，不占据首页视觉中心。

### 13.2 入口

- 账本首页右上角“＋”。
- 识别失败卡片的“补充金额”。
- 待确认交易的“编辑并入账”。

### 13.3 默认流程

1. 默认选中“支出”，顶部小按钮切换收入。
2. 首屏突出金额数字键盘和常用分类。
3. 选择分类后可直接保存。
4. 高级字段折叠：商户、日期时间、支付平台、支付方式、备注、是否计入统计。

### 13.4 校验

- 金额必须大于 0，保存时由方向决定正负语义。
- 支持精确到分，不支持浮点存储。
- 分类必填；未选择时默认“其他”，但需在保存按钮附近明确显示。
- 手动交易的来源标记为 `MANUAL`，不得参与自动捕获去重，除非用户主动选择合并。

---

## 14. 账本首页

### 14.1 布局

```text
          2026 年 8 月

          ¥ 2,431.50
            本月支出

今日 ¥68.50       预算剩余 ¥568.50

今天                              ¥68.50
☕ 瑞幸咖啡                        -¥16.00
   咖啡奶茶

🍜 一食堂                          -¥12.00
   正餐
```

### 14.2 规则

- 主数字为本月支出。
- 收入和结余弱化展示，不与本月支出争夺视觉焦点。
- 今日支出和预算剩余以轻量信息展示。
- 账单按日期分组；首页列表仅显示图标、商户、二级分类和金额。
- 月份通过左右滑动切换；V1 不要求月份下拉选择器。
- 右上角为搜索和手动记账。

### 14.3 快捷操作

- 左滑账单：编辑、删除。
- 删除后显示 Snackbar：`已删除“瑞幸咖啡 ¥16”  撤销`。
- 撤销窗口建议 5 秒（工程建议）；之后保持软删除记录，统计立即排除。
- 点击账单打开 Bottom Sheet，不跳转到复杂详情页。

---

## 15. 账单详情

### 15.1 展示字段

- 商户 / 收款方。
- 金额和收支方向。
- 一级、二级分类。
- 平台。
- 支付方式（允许未知）。
- 交易时间。
- 备注。
- 来源：自动 / 手动 / 导入。
- 交易状态：正常、待确认、已退款、部分退款等。
- 退款金额和实际净支出（如适用）。

### 15.2 操作

- 编辑金额、商户、分类、时间、支付方式和备注。
- 修改分类后可创建商户规则。
- 删除并提供撤销。
- 对转账标记为普通收入 / 支出、内部转移、AA 回款或忽略统计。
- 查看关联退款；用户可以手动更改退款关联。

---

## 16. 搜索与筛选

### 16.1 入口与范围

入口位于账本首页右上角。V1 支持全局本地搜索：

- 商户名。
- 分类名。
- 金额文本。
- 日期 / 月份。
- 支付平台。
- 备注。

### 16.2 筛选项

- 时间范围。
- 分类。
- 金额范围。
- 收入 / 支出。
- 支付平台。
- 来源（自动 / 手动 / 导入）。
- 状态（正常 / 待确认 / 退款 / 已删除不默认显示）。

### 16.3 排序

- 默认按交易时间倒序。
- 可切换金额从高到低 / 从低到高。
- 搜索与筛选完全本地执行。

---

## 17. 统计

### 17.1 页面结构

- 月 / 年维度切换。
- 顶部显示期间总支出。
- 第一模块为支出趋势。
- 第二模块为分类排行与横向条形图，不使用饼图作为主图。
- 收入和结余作为次级模块。

### 17.2 统计口径

- 默认只统计有效且 `includeInStats = true` 的交易。
- 退款以实际净支出口径计入分类支出。
- 内部转账不计入收入或支出。
- 红包默认计入收入；用户可设为不计入。
- 待确认交易默认不进入正式统计，但可单独显示数量提示。
- 软删除交易不计入统计。
- 跨期退款同时保留原始资金时间和净支出影响；V1 图表默认在退款发生日反映退款，并在原交易详情显示累计净支出。

### 17.3 下钻

点击趋势点或分类条形图，进入对应时间 / 分类的交易列表，沿用搜索页筛选组件。

---

## 18. 预算

### 18.1 范围

- V1 固定自然月：每月 1 日至月底。
- 支持月总预算和分类预算。
- 不支持自定义周期。

### 18.2 计算规则

- 使用有效净支出计算，不含收入、内部转账、忽略统计项和已删除项。
- 部分退款后以原支出减累计退款计算。
- 分类预算按交易最终分类计算；用户改分类后实时重算。

### 18.3 提醒

固定阈值：80%、100%、120%。

- 总预算和每个分类预算分别计算。
- 同一预算、同一周期、同一阈值只主动提醒一次。
- 用户因删除或退款回落后再次越过同一阈值，默认不重复提醒。
- 通知权限关闭时仅在应用内展示状态。
- 首页显示预算剩余；超支后显示超出金额。

---

## 19. 退款、部分退款、红包、转账与 AA

### 19.1 全额退款

数据库保留原消费和退款两个资金事件，UI 合并展示：

```text
淘宝                 ¥199.00
状态：已全额退款
实际支出：¥0.00
```

匹配优先级：

1. 平台订单标识摘要一致。
2. 用户手动指定。
3. 平台 + 商户 + 原金额 + 时间范围的高可信匹配。
4. 无法唯一匹配时，退款进入待确认，不自动绑定。

### 19.2 部分退款

```text
原始支出：¥100.00
已退款：  ¥30.00
实际支出：¥70.00
状态：部分退款
```

- 支持同一原交易多次部分退款。
- 累计退款不得静默超过原始支出；超出时进入异常状态等待处理。

### 19.3 红包

- 收到红包默认记录为收入，分类为“红包”。
- 用户可设置单笔或默认规则为“不计入收入统计”。
- 发出红包默认按转出处理并进入待确认，避免直接等同普通消费。

### 19.4 转账

- V1 所有自动捕获的转账默认进入待确认。
- 用户可标记为：普通支出、普通收入、内部转移、AA 回款、不计入统计。
- V1 不管理账户余额，因此“内部转移”只影响统计口径，不做余额双边记账。

### 19.5 AA 回款

- 第一版不自动匹配。
- 用户可在待确认中标记为 AA 回款，并可选关联原消费。
- 关联后原消费显示“AA 收回”和实际个人支出；数据库仍保留两笔资金事件。

---

## 20. 数据备份、恢复、导入与导出

### 20.1 Local First 原则

- 本地数据库是唯一核心数据源。
- 无账号、无网络时，自动记账、手动记账、搜索、统计、预算、备份和导出均可用。
- 云同步只能复制或合并本地数据，不能成为应用启动或查看账本的前置条件。

### 20.2 手动备份

备份至少包含：

- 交易、退款关联、分类、商户规则、预算、用户设置、解析规则版本和必要迁移元数据。
- 不包含诊断日志、临时页面快照或系统授权状态。

要求：

- 使用 Android 系统文件选择器由用户指定位置。
- 文件包含格式版本、创建时间、应用版本、记录计数和完整性校验。
- 建议采用加密备份；若 V1 提供密码加密，必须明确无法找回密码。若未实现加密，导出前需显著提示文件含敏感财务数据。
- 备份过程使用一致性快照，禁止得到一半新数据、一半旧数据。

### 20.3 恢复

恢复前依次执行：

1. 文件类型与格式版本校验。
2. 完整性校验。
3. 数据库迁移预演。
4. 展示备份时间、记录数和恢复模式。
5. 自动创建当前数据的安全备份。
6. 在事务中恢复；失败时回滚。

V1 默认提供“替换当前数据”，不做复杂合并。恢复操作必须二次确认。

### 20.4 CSV / Excel 导出

V1 支持 CSV 与 XLSX。建议字段：

```text
交易ID, 日期时间, 方向, 原始金额, 退款金额, 实际金额,
商户, 一级分类, 二级分类, 平台, 支付方式,
来源, 状态, 是否计入统计, 备注
```

- CSV 使用 UTF-8 BOM，保证常见中文 Excel 环境打开正常。
- XLSX 使用明确的日期和金额单元格类型，不把所有内容写成文本。
- 用户可选择时间范围和筛选条件。
- 导出不包含内部置信度、原始节点文本或诊断信息。

### 20.5 外部账单导入

V1 的“导入”仅指恢复默迹备份；不承诺直接解析微信 / 支付宝官方账单文件。后者作为 V1.x 独立需求处理，避免与备份恢复混为一谈。

---

## 21. 云同步路线

### 21.1 V1

- 不提供官方云同步。
- 支持本地文件备份和恢复。
- 所有核心功能不依赖登录。

### 21.2 V1.x

- 优先支持用户自己的 WebDAV / 云盘存储。
- 上传前在设备端加密，云端只保存密文。
- 提供手动备份优先，自动备份可配置为仅充电 / Wi-Fi。
- 仍以本地数据库为权威副本。

### 21.3 V2+

只有在有持续用户需求和可承担运营成本后，才评估官方同步：

- 可选账号，不登录仍完整可用。
- 端到端加密。
- 多设备增量同步、冲突检测和可回滚。
- 服务停用时提供完整数据导出与迁移方案。

---

## 22. 隐私、安全与诊断日志

### 22.1 隐私承诺

- 交易识别、分类和统计默认全部在本地完成。
- V1 不上传消费数据、商户、金额、页面文本或诊断原文。
- 不植入广告 SDK，不用消费数据做广告画像。
- 不读取密码、验证码、聊天正文、联系人或浏览器敏感存储。
- 权限目的发生变化时必须重新披露并取得同意。

### 22.2 数据最小化

- 只对微信 / 支付宝白名单包名的疑似支付页面做短时处理。
- 页面节点文本仅在内存中解析，形成结构化字段后立即释放。
- 数据库不保存完整 Accessibility 节点树或截图。
- 订单号如用于去重，优先保存不可逆摘要。

### 22.3 本地存储

- 使用应用私有目录保存数据库。
- 敏感配置使用 Android Keystore 保护；备份密钥不得明文写入普通偏好设置。
- 禁止将金额、商户、备注写入常规系统日志或崩溃报告。
- 第三方 SDK 默认禁止；引入前需做数据流审计。

### 22.4 最近任务隐私

- V1 不做应用锁。
- 设置中提供“隐藏最近任务预览”，由用户选择。
- 开启后多任务缩略图显示品牌占位页，不显示金额和账单。

### 22.5 诊断日志

普通模式只保存：

- 时间。
- 平台。
- 页面类型代码。
- 处理阶段。
- 成功 / 失败状态。
- 非敏感错误码。
- 解析器版本。

不得保存金额、商户、备注、完整通知文本和节点树。

开发者调试模式：

- 必须由用户显式开启并显示倒计时。
- 最多开启 24 小时，期满自动关闭并删除详细内容。
- 导出前允许用户预览和再次确认。
- 详细日志仍应优先脱敏。

---

## 23. 设置

### 23.1 自动记账

- 微信捕获状态。
- 支付宝捕获状态。
- 无障碍服务状态与重新开启入口。
- 通知辅助识别开关。
- 悬浮卡片开关及显示时长：关闭 / 2 秒 / 3 秒 / 5 秒 / 保持。
- 后台运行检查与厂商引导。
- 最近识别摘要。

### 23.2 分类与规则

- 分类新增、改名、图标、排序和隐藏。
- 用户分类删除与迁移。
- 自动分类规则查看、搜索、编辑、启用 / 禁用和删除。

### 23.3 预算

- 总预算。
- 分类预算。
- 提醒总开关；阈值固定为 80% / 100% / 120%。

### 23.4 数据

- 创建备份。
- 从备份恢复。
- CSV 导出。
- Excel 导出。
- 数据说明与格式版本。

### 23.5 外观与隐私

- 跟随系统 / 浅色 / 深色。
- 隐藏最近任务预览。
- 诊断日志与开发者调试模式。
- 隐私说明、权限用途和开源许可证（如适用）。

---

## 24. UI 与交互原则

### 24.1 视觉

- 大数字建立主次；本月支出是账本首页唯一视觉主角。
- 使用留白和排版分组，减少卡片、边框和装饰性图表。
- 浅色与深色均为一等主题，不采用简单反色。
- 金额对齐，正负方向在符号、颜色和文字上至少使用两种线索，不能只依赖颜色。
- 分类图以横向条形图为主，提高比较效率。

### 24.2 交互

- 高频动作一步完成；高级字段渐进展开。
- 正常自动记账不打断用户，不强制打开主应用。
- 所有删除操作提供撤销；不可逆恢复和数据替换必须二次确认。
- 异常信息说明“发生了什么、影响什么、如何修复”，避免技术错误堆栈。
- 置信度不以百分比展示；用“已记账 / 待确认 / 未识别金额”等用户语言表达。
- 动画服务于状态变化，遵循系统减少动态效果设置。

### 24.3 可访问性

- 关键文本和触控区域符合 Android 可访问性建议。
- 支持系统字号放大，金额和操作按钮不得截断。
- 图标提供内容描述。
- 深色模式保证对比度。
- 不以手势作为唯一操作路径；左滑编辑 / 删除必须在详情页提供等价按钮。

---

## 25. 数据模型建议

### 25.1 Transaction

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID/String | 本地稳定 ID |
| direction | EXPENSE/INCOME/TRANSFER | 资金方向 |
| amountMinor | Long | 原始金额，最小货币单位 |
| currency | String | 默认 CNY |
| merchantRaw | String? | 原始商户显示值 |
| merchantNormalized | String? | 规则匹配规范名 |
| categoryId | FK | 最终分类 |
| platform | WECHAT/ALIPAY/MANUAL/IMPORT/OTHER | 来源平台 |
| paymentMethod | Enum | 允许 UNKNOWN |
| occurredAt | Instant | 真实交易时间 |
| capturedAt | Instant? | 捕获时间 |
| source | AUTO/MANUAL/IMPORT | 创建来源 |
| status | Enum | 正常、待确认、失败、退款等 |
| includeInStats | Boolean | 是否进入统计 |
| note | String? | 用户备注 |
| confidence | Float? | 仅内部使用 |
| parserVersion | String? | 解析器版本 |
| dedupeKey | String? | 唯一 / 索引 |
| orderRefHash | String? | 可选订单标识摘要 |
| deletedAt | Instant? | 软删除时间 |
| createdAt/updatedAt | Instant | 审计时间 |

### 25.2 CaptureEvent

仅保存结构化、最小化的处理证据：

- id、platform、eventType、pageType、receivedAt。
- parserVersion、resultCode、candidateId。
- contentHash / pageInstanceHash，不保存完整页面文本。
- retentionUntil，用于自动清理。

### 25.3 TransactionCandidate

- id、platform、方向候选、金额候选、商户候选、时间。
- 字段级置信度、总体置信度、冲突码。
- 状态、dedupeKey、关联 CaptureEvent ID 列表。
- 最终 transactionId 或忽略原因。

### 25.4 RefundLink

- refundTransactionId。
- originalTransactionId。
- linkedAmountMinor。
- matchMethod：ORDER_ID / AUTO_HEURISTIC / USER。
- matchConfidence。

### 25.5 Category

- id、parentId、name、icon、sortOrder。
- origin：SYSTEM / USER。
- hidden、deletedAt。

### 25.6 MerchantRule

- id、pattern、normalizedBrand、matchType：EXACT / BRAND / KEYWORD。
- categoryId、origin：SYSTEM / USER。
- enabled、priority、lastUsedAt、createdAt、updatedAt。

### 25.7 Budget

- id、periodMonth、categoryId（空表示总预算）。
- limitMinor、currency。
- notified80、notified100、notified120。

### 25.8 BackupManifest

- formatVersion、appVersion、schemaVersion、createdAt。
- recordCounts、checksum、encryptionMetadata。

### 25.9 约束与索引

- `Transaction.dedupeKey` 对非空有效记录建立唯一约束或等价幂等表。
- `occurredAt`、`categoryId + occurredAt`、`merchantNormalized` 建索引。
- `RefundLink.refundTransactionId` 唯一。
- 金额累计退款约束在事务内检查。
- 删除系统分类只允许设置 `hidden = true`。

---

## 26. 关键状态机

### 26.1 捕获候选状态机

```text
DETECTED
  ↓
COLLECTING ──超时/证据不足──→ PARSE_FAILED
  ↓
PARSED
  ├──重复──→ DUPLICATE_SUPPRESSED
  ├──失败支付──→ PAYMENT_FAILED
  ├──高可信──→ AUTO_POSTED → FINALIZED
  └──不确定──→ PENDING_REVIEW
                    ├──确认/补充──→ CORRECTED → FINALIZED
                    └──忽略──→ IGNORED
```

### 26.2 交易生命周期

```text
ACTIVE
  ├──用户修改──→ ACTIVE（更新审计时间，可生成商户规则）
  ├──部分退款──→ PARTIALLY_REFUNDED
  ├──全额退款──→ FULLY_REFUNDED
  └──删除──→ DELETED ──撤销──→ ACTIVE
```

### 26.3 服务健康状态

```text
UNKNOWN → HEALTHY
   ├──权限关闭──→ PERMISSION_DISABLED
   ├──服务断开──→ SERVICE_DISCONNECTED
   ├──后台受限──→ BACKGROUND_RESTRICTED
   └──解析异常激增──→ PARSER_DEGRADED

任一异常修复并通过自检 → HEALTHY
```

---

## 27. 验收标准

### 27.1 自动捕获

- [ ] 无障碍未开启时不尝试读取第三方页面，手动记账仍可用。
- [ ] 已适配微信和支付宝成功页可生成金额正确的候选交易。
- [ ] 同一支付页面产生多次事件时只形成一条有效交易。
- [ ] Accessibility 与通知同时命中时只形成一条交易并合并证据。
- [ ] 商户缺失但金额可靠时自动入账为“未知商户”。
- [ ] 金额缺失时不入账，并显示包含商户小字的补录提示。
- [ ] 失败和取消支付不进入账本。
- [ ] 支付方式不可获取时保存 `UNKNOWN`，不猜测。
- [ ] 转账默认进入待确认。

### 27.2 反馈与纠正

- [ ] 自动入账后默认显示 3 秒卡片，且可改为关闭 / 2 / 3 / 5 秒 / 保持。
- [ ] 卡片支持修改和撤销，默认不震动。
- [ ] 普通界面不显示置信度百分比。
- [ ] 用户修改分类后可选择精确商户或品牌范围并生成规则。
- [ ] 用户规则覆盖系统规则，且可编辑、禁用和删除。

### 27.3 账本与特殊交易

- [ ] 首页显示本月支出、今日支出、预算剩余和按日分组账单。
- [ ] 左右滑动可切换月份。
- [ ] 点击账单打开 Bottom Sheet；左滑可编辑 / 删除；删除可撤销。
- [ ] 全额退款 UI 显示实际支出 0，数据库保留原交易与退款事件。
- [ ] 部分退款正确显示原始金额、累计退款和净支出。
- [ ] 多次部分退款累计不得无提示超过原金额。
- [ ] 红包默认收入且可排除统计。
- [ ] AA 回款可手动关联原消费。

### 27.4 搜索、统计与预算

- [ ] 可按商户、分类、金额、时间、平台和备注搜索。
- [ ] 支持时间、分类、金额范围、方向、平台、来源和状态筛选。
- [ ] 月 / 年趋势与分类条形图口径一致。
- [ ] 退款、内部转账、忽略统计和软删除的统计口径符合本文定义。
- [ ] 总预算和分类预算按自然月计算。
- [ ] 80% / 100% / 120% 各阈值每周期最多主动提醒一次。

### 27.5 Local First 与数据安全

- [ ] 飞行模式且未登录时，除云同步外所有 V1 功能可用。
- [ ] 创建备份后可在干净安装中完整恢复交易、分类、规则、预算和设置。
- [ ] 损坏、版本不支持或校验失败的备份不得覆盖现有数据。
- [ ] 恢复失败可回滚到恢复前状态。
- [ ] CSV 中文可被常见 Excel 环境正确打开，金额和日期准确。
- [ ] XLSX 使用正确数据类型。
- [ ] 数据库和常规日志中不存在完整页面节点树、密码、验证码或无关页面正文。
- [ ] 普通诊断日志不含金额、商户和备注。

### 27.6 权限与健康状态

- [ ] 每项特殊权限申请前都有独立、清楚的用途披露。
- [ ] 用户拒绝任一可选权限后应用不崩溃，并给出对应降级体验。
- [ ] 服务异常时首页提醒，正常时不显示状态占位。
- [ ] 最近任务隐藏开关生效，关闭时恢复正常预览。

---

## 28. 测试矩阵建议

### 28.1 场景维度

- 微信：线下扫码、付款码、线上支付、转账收 / 支、红包、退款、失败 / 取消。
- 支付宝：线下扫码、付款码、线上支付、转账收 / 支、红包类收入、退款、失败 / 取消。
- 字段异常：无商户、无金额、支付方式缺失、多金额文本、页面快速退出。
- 并发异常：相同金额连续支付、应用重启、服务重连、通知延迟到达。

### 28.2 环境维度

- Android 7.0（API 24）最低支持验证，以及当前主要 Android 大版本。
- 主流厂商系统及其后台限制差异。
- 微信 / 支付宝多个主流版本。
- 浅色 / 深色、不同系统字体大小、离线状态、低电量模式。

### 28.3 样本证据

每个解析回归样本至少包含：

- 平台、APP 版本、Android / 厂商版本。
- 场景和预期状态。
- 脱敏后的节点结构 / 字段证据。
- 预期候选字段、置信度区间和去重结果。
- 是否允许进入自动入账。

---

## 29. 风险与能力边界

| 风险 | 影响 | 应对 |
|---|---|---|
| 微信 / 支付宝页面改版 | 捕获率下降或字段错位 | 版本化 Parser、回归样本、失败率监控、保守分流 |
| 节点树内容不完整或瞬时失效 | 漏金额 / 商户 | 多事件短时聚合、可选通知校验、失败补录 |
| 厂商后台限制 | 服务中断、漏单 | 健康检查、厂商引导、异常提示 |
| 连续相同支付 | 误去重 | 两级指纹、时间窗、页面实例和订单证据 |
| 退款无法唯一匹配 | 净支出错误 | 不自动绑定，进入待确认 |
| 特殊权限引发用户不信任 | 转化和留存下降 | 独立披露、最小权限、Local First、无广告 SDK |
| Google Play 审核 | 上架或更新延迟 / 驳回 | 不声明为辅助工具；完整披露、明确同意、准确提交 Accessibility 声明与演示 |
| 备份文件泄露 | 财务隐私暴露 | 应用私有存储、加密备份路线、导出警告、用户指定位置 |
| 免费无广告的长期维护成本 | 适配停滞 | 优先本地规则、用户自有云、控制服务成本 |

### 29.1 明确能力边界

- Android 无障碍服务获取的是应用暴露的可访问性节点层级，不是万能截图 OCR；部分自定义控件或页面可能无法提供完整字段。
- 支付成功后立即退出、系统杀死服务、页面改版或权限关闭时可能漏单。
- 自动记账仅覆盖已适配的微信 / 支付宝场景，不等于所有经过二者扣款的业务都必然可见。
- 银行 APP、现金、Apple Pay、Samsung Pay 等不在 V1 自动捕获范围。
- iOS 因系统限制不提供 Android 等价体验。
- 鸿蒙及其兼容层需要单独验证，不在 V1 承诺范围。
- Google Play 上架需按其 AccessibilityService 政策进行用途申报、应用内显著披露和用户明确同意；审核通过不是技术实现本身能够保证的结果。

---

## 30. 开发里程碑建议

### M0：风险验证（1～2 周）

- 微信 / 支付宝各完成一个真实成功页的脱敏节点样本采集。
- 验证金额、商户、状态提取及 30 秒事件聚合。
- 在至少 3 类 Android / 厂商环境验证服务生命周期。
- 输出首版场景兼容矩阵和 Play 合规清单。

### M1：核心自动记账 MVP（2～4 周）

- Parser 注册框架、候选模型、去重和置信度分流。
- Room / SQLite 数据层与核心状态机。
- 账本首页、详情、修改、删除 / 撤销、手动记账。
- 轻量卡片和服务健康提示。

### M2：完整账务能力（2～3 周）

- 分类体系、用户商户规则、待确认。
- 退款、部分退款、红包、转账和 AA 手动关联。
- 搜索与筛选。

### M3：V1 产品闭环（2～3 周）

- 统计、预算和提醒。
- 备份、恢复、CSV / XLSX 导出。
- 设置、主题、隐私和诊断。
- 首次启动与完整权限降级流程。

### M4：发布前验证（持续）

- 真实设备 / 厂商 / Android 版本兼容矩阵。
- 微信 / 支付宝场景回归。
- 备份灾难恢复测试。
- 隐私数据流审计、权限披露审查、商店材料与演示视频。

---

## 31. 后续路线图

### V1.1：稳定性与适配

- 扩展微信 / 支付宝页面矩阵。
- 脱敏回归样本工具。
- 解析规则版本管理和回滚。
- 外部官方账单 CSV 导入评估。

### V1.2：用户自有云

- WebDAV / 云盘端到端加密备份。
- 自动备份策略、历史版本和恢复预览。
- 仍保持无账号完整可用。

### V1.5：智能整理

- AA 回款辅助匹配。
- 自定义预算周期和提醒阈值。
- 更强搜索与自然语言查询的本地优先方案。
- 拆账和批量编辑。

### V2：可选多设备能力

- 官方端到端加密同步的商业与成本验证。
- 多设备冲突处理和审计历史。
- 在不损害 Local First 的前提下探索更多支付来源。

---

## 32. Android 实现约束与官方参考

以下不替代详细技术设计，但属于 PRD 对实现的硬约束：

- `AccessibilityService` 若需读取窗口节点层级，服务配置需要声明相应能力；节点源可能为空或失效，代码必须容错。
- 通知访问使用用户单独授权的 `NotificationListenerService`，只作为可选辅助来源。
- 悬浮卡片应检测系统是否允许在其他应用上层显示，并提供无权限降级路径。
- 文件备份 / 恢复和导出优先使用系统文件选择器，让用户明确选择目标文件。
- 公开分发前必须重新核对当时有效的 Android 和应用商店政策。

官方参考：

- [Android：创建无障碍服务](https://developer.android.com/guide/topics/ui/accessibility/views/service?hl=zh-CN)
- [Android：NotificationListenerService API](https://developer.android.com/reference/android/service/notification/NotificationListenerService)
- [Google Play：AccessibilityService API 使用政策](https://support.google.com/googleplay/android-developer/answer/10964491?hl=en)

---

## 33. 需求冻结摘要

V1 的产品判断标准不是功能数量，而是以下闭环是否成立：

```text
一次真实成功支付
    ↓
只形成一条正确交易
    ↓
高可信时无感入账
    ↓
异常时可理解、可补录、可纠正
    ↓
数据只在本地也能长期保存、统计、备份和迁移
```

在这一闭环稳定前，不优先投入账号体系、官方云、复杂资产管理、云端 AI、社交或商业化功能。
